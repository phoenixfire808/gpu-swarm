# GPU Pool — full host stack (scheduler, portal, Discord bot, CLI)
# Supported: Windows Python 3.10–3.12 (64-bit). Prefer 3.12.
# Python 3.13: optional/experimental — prefer 3.12 for joiners and CUDA torch.
# Joiners/contribute/utilize without Discord/torch: requirements-joiner.txt
# Optional CUDA torch: requirements-cuda.txt via scripts\install_joiner_deps.ps1 -WithTorchCuda

httpx>=0.28.1,<0.29
python-dotenv>=1.0.1,<1.2
fastapi>=0.115.0,<0.116
uvicorn>=0.34.0,<0.35
pydantic>=2.10.0,<2.12
aiosqlite>=0.20.0,<0.22
discord.py>=2.4.0,<2.6
psutil>=5.9.8,<7
# Desktop UI (also in requirements-joiner / requirements-app):
customtkinter>=5.2.2,<6
