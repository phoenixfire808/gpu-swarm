# Desktop joiner + worker path (bundled into GPUPool.exe).
# Same pins as requirements-joiner.txt — no torch, no discord.
# Prefer scripts\install_joiner_deps.ps1 over ad-hoc pip --user.

httpx>=0.28.1,<0.29
python-dotenv>=1.0.1,<1.2
fastapi>=0.115.0,<0.116
uvicorn>=0.34.0,<0.35
customtkinter>=5.2.2,<6
psutil>=5.9.8,<7
pydantic>=2.10.0,<2.12
aiosqlite>=0.20.0,<0.22
